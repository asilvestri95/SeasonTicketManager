services:
  app:
    build: .
    ports:
      - "3001:3001"
    volumes:
      - db_data:/app/server/data
    environment:
      - NODE_ENV=production
      - JWT_SECRET=change-this-to-a-long-random-secret
    restart: unless-stopped

volumes:
  db_data:
